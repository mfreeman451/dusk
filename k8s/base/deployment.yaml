apiVersion: apps/v1
kind: Deployment
metadata:
  name: dusk-node
  namespace: dusk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dusk-node
  template:
    metadata:
      labels:
        app: dusk-node
    spec:
      # Init containers run before the main container
      initContainers:
        - name: setup-dusk
          image: ghcr.io/mfreeman451/dusk-node:v0.0.7
          command:
            - sh
            - -c
            - |
              cp -r /opt/dusk/bin/* /dusk-bin/ && \
              chown -R 1000:1000 /dusk-bin/ && \
              mkdir -p /home/dusk/.dusk/rusk-wallet && \
              chown -R 1000:1000 /home/dusk/
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            - name: dusk-bin
              mountPath: /dusk-bin
            - name: dusk-home
              mountPath: /home/dusk
      # Main container
      containers:
        - name: dusk-node
          image: ghcr.io/mfreeman451/dusk-node:v0.0.7
          imagePullPolicy: Always
          volumeMounts:
            - name: dusk-bin
              mountPath: /opt/dusk/bin
            - name: dusk-state
              mountPath: /opt/dusk/state
            - name: dusk-conf
              mountPath: /opt/dusk/conf
            - name: dusk-home
              mountPath: /home/dusk
            - name: secrets
              mountPath: /config
              readOnly: true
          env:
            - name: PATH
              value: /opt/dusk/bin:/usr/bin:/usr/local/bin:/sbin:/bin
            - name: HOME
              value: /home/dusk
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
      volumes:
        - name: dusk-bin
          emptyDir: {}
        - name: dusk-state
          persistentVolumeClaim:
            claimName: dusk-state
        - name: dusk-conf
          emptyDir: {}
        - name: dusk-home
          persistentVolumeClaim:
            claimName: dusk-wallet
        - name: secrets
          secret:
            secretName: dusk-node-secret